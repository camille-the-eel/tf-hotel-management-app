
                <div class = constrainer2>
                <table class="table is-fullwidth is-striped is-narrow is-hoverable">
                <thead>
                  <tr>
                  <th>RES #</th>
                  <th>NAME</th>
                  <th>DATE IN</th>
                  <th>DATE OUT</th>
                  <th>ROOM #</th>
                  </tr>
                </thead>
                <tbody>
                <%for (var i = 0; i < Reservation.length; i++){%>
                
                <tr>
                <td><%=Reservation[i].id%></td>
                <td><%=Reservation[i].Guest.first_name%> <%=Reservation[i].Guest.last_name%></td>
                <td><%=Reservation[i].date_in%></td>
                <td><%=Reservation[i].date_out%></td>
                <td><%=Reservation[i].RoomId%></td>
                <td>
                  
                    <div class="field is-horizontal">
                      <div class="field-body">
                        <div class="field has-addons">
                          <div class = "control">
                              <input data= <%=Reservation[i].id%> class = "button check-in" type = "submit" value = "CHECK IN">
                          </div>
                        </div>
                      </div>
                    </div>
                 
                </td>
                </tr>

                <%}%>
                </tbody>
                    </table>
                    </div>